<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.etherfilter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Activity_result"></activity>
        <activity android:name=".Activity_search" />
        <activity
            android:name=".Activity_account"
            android:parentActivityName=".Activity_munu">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activity_munu" />
        </activity>
        <activity
            android:name=".Activity_report"
            android:parentActivityName=".Activity_munu">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Activity_munu" />
        </activity>
        <activity android:name=".Activity_munu" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@drawable/edittext_edge" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>